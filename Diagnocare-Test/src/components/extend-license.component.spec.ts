/**
 * REMOVED — the Extend Licence screen no longer exists in the individual application.
 * Licence extension is performed in the shared Diagnocare application; this app only
 * reads licence state. This file is a placeholder and can be deleted from source control.
 */
describe('ExtendLicenseComponent', () => {
  it('is intentionally removed from the individual application', () => {
    expect(true).toBe(true);
  });
});
