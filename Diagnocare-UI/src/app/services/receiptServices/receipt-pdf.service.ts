/**
 * @deprecated This service has been removed.
 *
 * Receipt PDF generation is now handled server-side.
 * Use ReceiptService.generateReceiptPdf(receiptId) to request a PDF blob
 * from the backend (api/receipt/GenerateReceiptPdf?receiptId=X).
 *
 * This file is kept as a placeholder only so existing imports do not break
 * during the transition. It can be deleted once all references are cleaned up.
 */
